"""Telemetry REST router tests — retired post-Task-4.

The /api/telemetry router temporarily proxies the new ParameterCache
during the transition. Task 6 deletes it in favor of an explicit
``/api/parameters/...`` surface; tests for that live shape will land then.
This file is intentionally empty.
"""
